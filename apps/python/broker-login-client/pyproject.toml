[project]
name = "calle-broker-login-client-python"
version = "0.0.0"
description = "Minimal CALL-E broker login MCP client app."
requires-python = ">=3.10"
dependencies = [
  "httpx>=0.28.1",
]

[dependency-groups]
dev = [
  "pytest>=9.0.0",
]
